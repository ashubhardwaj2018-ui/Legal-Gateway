{
  "name": "@workspace/scripts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "hello": "tsx ./src/hello.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "seed-priority-cities": "tsx ./src/seed-priority-cities.ts",
    "fix-priority-cities": "tsx ./src/fix-priority-cities.ts",
    "generate-sitemap-index": "node ./generate-sitemap-index.mjs"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tsx": "catalog:",
    "xlsx": "^0.18.5"
  },
  "dependencies": {
    "@types/pg": "^8.18.0",
    "pg": "^8.20.0"
  }
}
